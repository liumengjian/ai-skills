---
name: jjb-page-extra-area
description: 定义 JJB 项目页面右上角操作区规范。在放置新建、创建、新增、导入导出等按钮时使用。必须放在 PageLayout.extra 中。
---

# 页面布局与通用 UI 规范

## 页面右上角操作区

- 以下按钮 **必须** 放在 `PageLayout.extra`（右上角）：

  - 新建

  - 创建

  - 新增

  - 添加

  - 导入数据

  - 导出数据
