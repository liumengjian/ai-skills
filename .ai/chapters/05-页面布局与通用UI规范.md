# 五、页面布局与通用 UI 规范

### 1. 页面 Layout

- 除提示词明确不需要的情况下，所有页面 **必须** 使用：

  `@cqsjjb/jjb-react-admin-component/PageLayout` 组件

  必须严格参考对应 MD 文档或 d.ts 文件！不得猜测组件属性！

- 禁止自定义页面根容器（除提示语明确要求的除外）

### 2. 页面右上角操作区

- 以下按钮 **必须** 放在 `PageLayout.extra`（右上角）：

  - 新建

  - 创建

  - 新增

  - 添加

  - 导入数据

  - 导出数据
