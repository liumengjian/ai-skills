# 二十、jjb-cmd 命令行工具使用指南

## 3. 登录授权

**⚠️ 重要前置条件**：执行 `jjb-cmd push` 或 `jjb-cmd publish` 命令之前，**必须先执行 `jjb-cmd auth` 进行登录授权**，否则无法执行这些命令。

推送前端包、发布云组件都需要登录授权通过才能执行命令。

### 3.1 命令格式

```bash
jjb-cmd auth
```

### 3.2 使用说明

执行命令后，会以交互式方式提示输入用户名和密码。

**账号密码联系开发者获取！**
